%h1 #{t 'activerecord.models.company.other'}:
%ul
  - @companies.each do |company|
    %li
      = link_to company.name, company
      - if can? :manage, company
        = link_to "#{t 'pages.companies.edit'}", company
