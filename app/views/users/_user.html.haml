= link_to "#{user.email}", user_path(user)
= link_to image_tag(gravatar_url(user)), user_path(user.id)
= user.first_name
= user.last_name
- user.technologies.each do |technology|
  = technology.name
%br
